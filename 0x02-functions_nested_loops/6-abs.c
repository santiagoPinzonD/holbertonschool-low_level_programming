#include "holberton.h"

/**
* _abs - returns y value of a number
* @r: the number to check
* Return: y number
* On zero 0.
*/

int _abs(int r)
{
	
	if (r >= 0)
{
	return (r);
}
	else
{
	return (-r);
}
}
